import { UseToastOptions } from '@chakra-ui/react';

export const toastValues: UseToastOptions = {
  size: 'sm',
  duration: 1500,
  isClosable: true,
  colorScheme: 'purple',
  containerStyle: { fontSize: 'sm', fontWeight: 'medium' },
};
